*** 이미지를 저장할 수 있습니다

** public과 assets 차이점
: public에 저장하는 이미지는 전역이미지로 favicon이나 HTML에 직접 로드할때 적합하고, assets에 저장하는 이미지는 import를 통해 모듈 형식으로 불러 올 수 있어서 빌드에 최적화되어있기때문에 컴포넌트 내에서 사용하는 이미지는 assets 에 모듈로 저장하는 것이 효율적입니다

->결론 : 프로젝트 구현할때 이미지는 assets에 저장하도록 합시다!